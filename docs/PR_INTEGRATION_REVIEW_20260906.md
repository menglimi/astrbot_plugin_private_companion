# PR 整合审查记录

日期：2026-09-06。整合分支：`codex/integrate-pr203-204-20260906`。

## 审查基线

- 主分支：`65bf3e4`，包含 NAI 直连工具可用性和内部 LLM 流式设置修复。
- PR #203：`9596c5a9879ea9db9f60d642dd07d64132bbcbdc`。
- PR #204：`79aa69bde1a7207047b475fa188a4a84c75f6831`。
- 本地合并提交：`e2f7fac`（#204）、`be3c584`（#203）。

## 发现与处理

1. #204 的旧身份锚点返回字符串，和主分支的 PromptSection 接口冲突。保留模块拆分，外层继续使用统一提示词段；保留固定称呼、人称消歧和显示名收敛行为。
2. #204 的版本号低于当前主分支。保留 6.4.5c，修正人格配置和照片测试的冲突，不恢复已移除的 PhotoPromptSection 接口。
3. #203 根据群成员数字 ID 直接改写私聊用户的 companion_memory，重复观察同一名场面还会刷新记忆时间。改为来源群内的临时互动线索，提示词明确单次表达不能证明长期偏好；验证其他群、其他说话人、过期和关闭功能的情况不会注入该线索。
4. #203 的异步画像桥接没有检查分项关闭状态，也没有任务生命周期管理；当前记忆插件不存在 record_group_moment_portrait 接口。移除这条不可用写入链路，保留本群提示词参考功能。
5. #203 新增的跨会话召回、回溯天数和本地兜底配置没有对应运行实现。暂不暴露这些无效设置，保留现有召回行为。
6. 将主插件的后台任务创建与取消方法接入 #204 的 BackgroundTaskManager，保留响应持久化任务跟踪和终止时取消行为。
7. 清理 #203 中只有换行符变化的文件。保留 SQLite 批量查询、WAL 调优、原子持久化和恢复失败可见性改动。

## 验证

使用 AstrBot 自带的 Python 3.12：`C:/Users/99505/.astrbot/backend/python/python.exe`。
设置 `PYTHONUTF8=1` 和 `PYTHONPATH=C:/Users/99505/.astrbot/backend/app`。

- SQLite、生命周期、身份锚点、人格设置、照片和管理命令：110 passed，28 subtests passed。
- 群聊互动线索、群上下文、面板设置、提示词缓存和身份归属：110 passed，7 subtests passed。
- 生命周期、最终响应持久化、打包、问题回归、MRO、配置派发、迁移和桥接：120 passed，9 subtests passed。
- 原工作树的注入协议、私聊身份归属和 TTS：58 passed。
- 架构与打包导入检查通过。工作树使用 CRLF 的文件按 cr-at-eol 规则检查空白。

各测试批次有重叠，数量不代表唯一测试总数。测试使用真实 AstrBot 依赖；没有运行真实消息投递或性能基准，不能据此确认 PR 描述中的性能提升百分比。

## 当前状态

整合结果仅在本地独立工作树中，尚未推送或合并 GitHub PR。原插件工作树及其未提交的注入协议、页面 API 等改动保留原状，没有纳入这两个 PR 的整合提交。
