# 任务提示词面板交接文档

## 交接范围

本次改动为“陪伴面板”新增统一的“提示词”管理入口，用于查看和维护插件内部任务模型使用的提示词。入口与“实验”等一级面板项同级，默认展示完整内置提示词。

## 最新要求（2026-09-10）

- “提示词”专指本插件内部的任务模型提示词；例如“工具结果转述（识屏）”“工具结果转述（合并转发）”等任务模型都必须纳入管理，不能只覆盖主链上的少数任务。
- 默认打开任务时直接展示该任务的完整内置提示词，不能只显示截断内容、概述或一两句说明。
- 完整提示词中的运行时用户资料、人格、历史、工具结果、图片观察等内容必须用明确的安全占位符表示（例如 `{{task_input}}`、`{{persona_context}}`、`{{conversation_context}}`、`{{tool_result}}`、`{{image_observations}}`、`{{runtime_metadata}}`）；不得把真实会话数据写入面板或配置。
- 不管理 AstrBot 主对话系统提示词、主对话人格提示词或普通聊天回复提示词；“全量”只表示本插件内部任务模型全量。
- 任务推进可使用更多子代并行协作，但所有子代都必须遵守本边界和本地测试约束。
- 只能操作本机 `127.0.0.1:6185` 测试实例；严禁访问、打开、登录或修改 `160.202.237.7:6185` 的 AstrBot。

这里的“提示词”有严格边界：

- 只管理本插件内部任务模型的提示词。
- 必须覆盖全部插件内部任务模型，包括“工具结果转述（识屏）”“工具结果转述（合并转发）”等任务。
- 不管理 AstrBot 主对话系统提示词、主对话人格提示词或普通聊天回复提示词。
- 用户自定义内容只会追加到对应的插件内部任务请求，不会改写或注入 AstrBot 主对话系统提示词。
- 只能操作本机 `127.0.0.1:6185` 测试实例；严禁访问、打开、登录或修改 `160.202.237.7:6185` 的 AstrBot。

## 当前实现

### 后端

- 新增 `task_prompt_registry.py`，登记 97 个任务、13 个分组。
- 注册表过滤 `astrbot_*` 主对话任务，只保留插件内部任务模型。
- 新增接口：
  - `GET /task-prompts`：返回分组、任务、完整内置提示词、当前自定义附加指令、继承关系和保存状态。
  - `POST /task-prompts/update`：支持单个或批量更新、继承、恢复默认和失败回滚；同一插件实例内的更新会串行保存，失败请求的回滚不会覆盖后续成功请求。
- `builtin_prompt` 返回与生产调用一致的完整固定内置提示词安全投影；仅将运行时字段替换为安全占位符，不是模板、摘要或截断内容。当前内容固定包含 `【任务身份】`、`【任务目标】`、`【动态输入】`、`【执行规则】`、`【输出契约】`、`【禁止事项】` 六段。`builtin_prompt_dynamic` 保持为 `true`，只表示提示词含运行时占位符，不表示内容被截断。
- 动态用户资料、人格状态、历史消息、工具结果和图片观察只在真实任务执行时注入，绝不展示或持久化真实值。
- 已接入以下调用链：
  - 通用 `_llm_call`、`_llm_tool_call`
  - TTS 增强
  - 视觉 Provider
  - 主动消息 Framework Agent
  - 单图 Framework Agent

### 前端

两套静态资源已同步：

- `pages/companion-panel/`
- `pages/陪伴面板/`

面板中“15 提示词”紧随“14 实验”，与最大父项保持同级。列表支持分组筛选、任务搜索、查看内置提示词、编辑附加指令、保存、恢复默认及状态反馈。

## 重点覆盖清单

注册表应始终覆盖插件内所有任务模型，不得只覆盖主链任务。当前已包含：

- 工具结果转述（识屏）
- 工具结果转述（合并转发）
- 视觉分析与图片理解
- TTS 文本整理与语音相关任务
- 主动消息候选、复核和发送决策
- 群聊观察、群聊唤醒和群图审核
- QQ 空间分享与内容整理
- 人格编辑、关系编辑和场景风格任务
- 创作、续写、大纲、审校和封面相关任务

新增内部任务时，必须同步在 `task_prompt_registry.py` 登记，并补充任务标识测试；若调用链使用了新的模型入口，还要把提示词注入接入对应调用点。

## 关键文件

- `task_prompt_registry.py`：任务清单、分组、完整固定内置提示词安全投影和自定义覆盖逻辑。
- `page_api.py`：提示词管理 API。
- `token_budget.py`：任务调用上下文与预算关联。
- `forward_message.py`、`llm_tool_actions.py`、`private_image.py`、`proactive_message.py`、`tts_enhancement.py`：主要任务模型调用接入点。
- `pages/companion-panel/index.html`、`app.js`、`app.css`：英文目录面板资源。
- `pages/陪伴面板/index.html`、`app.js`、`app.css`：中文目录面板资源。
- `tests/test_task_prompt_registry.py`：注册表覆盖与边界测试。
- `tests/test_task_prompt_api.py`：接口、批量更新、恢复默认和回滚测试。
- `tests/test_task_prompt_panel_ui.py`：面板静态契约测试。

## 已完成验证

2026-09-10 交付前使用 AstrBot bundled Python（`C:\Users\cmw147\AppData\Roaming\uv\tools\astrbot\Scripts\python.exe`）重新执行最终邻接测试，结果为：

```text
141 passed
59 subtests passed
1 warning
```

变基到 `upstream/main`（`9cf2d44`）之后，在同一集合上追加 `tests/test_hdsi_experiment.py` 与
`tests/test_group_social_contracts.py` 复跑，结果为：

```text
214 passed
59 subtests passed
1 warning
```

执行的测试文件为 14 个（变基后复跑另加上述 2 个，共 16 个）：

```text
tests/test_task_prompt_registry.py
tests/test_task_prompt_api.py
tests/test_task_prompt_panel_ui.py
tests/test_task_prompt_cache_prefixes.py
tests/test_token_task_identifiers.py
tests/test_group_image_review_config.py
tests/test_visual_provider_routing.py
tests/test_proactive_background_prompt_sections.py
tests/test_tts_prompt_section_authoring.py
tests/test_tool_prompt_sections.py
tests/test_page_api_route_bindings.py
tests/test_build_plugin_package.py
tests/test_release_layout.py
tests/test_prompt_authoring_static_check.py
```

覆盖范围包括：

- 任务注册表、完整内置提示词、精确字段/枚举契约和主对话排除边界
- API 单条/批量更新、恢复默认、保存失败回滚和并发更新隔离
- 双面板静态契约、任务标识长度契约、视觉缓存、视觉 Provider 路由、群图审核
- 主动背景提示词、TTS 提示词分区、工具提示词分区
- 页面路由绑定、插件打包、发布布局和提示词撰写静态检查

其中注册表专项为：

```text
22 passed
14 subtests passed
```

提示词功能四项（注册表、API、双面板、缓存前缀）为：

```text
40 passed
14 subtests passed
1 warning
```

打包与发布布局专项为：

```text
10 passed
1 warning
```

警告均来自 AstrBot 依赖中的 `audioop` 弃用提示，不是本功能失败。本轮没有重新执行整个仓库的全量测试，因此不沿用早先全量运行的通过/失败数字。

另已通过：

- 修改过的 Python 文件 `compileall`
- 两份 `app.js` 的 `node --check`
- 两套面板的 `index.html`、`app.js`、`app.css` SHA-256 一致性
- `_conf_schema.json` JSON 解析
- `scripts/ci_static_checks.py --package astrbot_plugin_private_companion --import-module story_migration_contract`
- `git diff --check`（只有 Git 的 LF/CRLF 转换提示，无空白错误）

### 本轮完整提示词复核（2026-09-10）

本轮断言确认每个注册任务都返回完整六段内置提示词和安全动态占位符，并确认主对话任务标识不会出现在目录或更新接口中。另逐项对照生产解析代码修正了以下任务的完整输出契约：

- `rest_wakeup_judge`：`score`、`should_reply`、`reason`
- `group_air_reply_guard`：只输出 `REPLY` 或 `SILENCE`
- `group_followup_judge`：只输出 `YES` 或 `NO`
- `group_nsfw_image_review`：`label=safe|adult_nsfw|disallowed|uncertain` 与 `confidence`
- `reaction_vision_verify`：`fit`、`description`、`reason`
- `smart_message_debounce`：`decision=complete|incomplete`、`confidence`、`reason`
- `smart_silence`：`decision=send|silent`、`confidence`、`reason`
- `group_question_wakeup_reply_review`：`decision=send|drop`、`reason`
- `provider_test`：文本和视觉测试均只回复“正常”

本机 AstrBot 测试实例已重启，Dashboard 已确认：

- “提示词”入口存在
- 任务总数为 97
- 两个“工具结果转述”任务均存在
- 默认完整内置提示词可以显示

已在本机 `127.0.0.1:6185` 完成桌面和 `390x844` 窄屏检查：

- 入口与“14 实验”同级显示，窄屏导航可横向滚动到“15 提示词”。
- 任务目录显示 97 个任务，列表可滚动到最后一个“代答消息转写”。
- “工具结果转述（识屏）”和“工具结果转述（合并转发）”的完整内置提示词、占位符、只读标识和任务范围说明正常。
- 临时保存附加指令后切换任务仍能恢复；二次确认“恢复默认”后回到“使用内置任务提示词”。
- 桌面和窄屏均未发现页面横向溢出、控件遮挡或编辑区布局跳动。
- 检查结束已将浏览器视口恢复为 `1440x900`，并关闭自动化浏览器。

## 本地运行约束

测试实例位于 `E:\1\陪伴系列\AstrBot`，Dashboard 只监听本机 `127.0.0.1:6185`。启动方式：

```powershell
cd E:\1\陪伴系列\AstrBot
astrbot run --reload
```

不要在 AstrBot WebUI 中更新、卸载或执行 `astrbot plug update/remove`；五个插件目录通过 Junction 连接真实源码，插件更新必须在各自 Git 仓库中完成。

## 当前工作区状态

当前分支：`codex/task-prompt-panel`。功能改动、测试与本交接文档已在同一提交 `6f26db7` 中归档，并已变基到 `upstream/main`（`9cf2d44`）之上。

用户已有文件必须保留，且不得纳入提交：

- `.save_calls.txt`
- `.tmp_page_api_diff.txt`
- `tests/test_portrait_panel_sync_route.py`

本轮审计临时文件已确认并删除；上述三个用户文件保持不变。

## 交付记录

1. 差异范围已核对，只提交本功能文件、测试与本交接文档；三个用户文件仍为未跟踪状态，未纳入提交。
2. 提交前已执行 `git diff --check`，并确认两套前端资源 SHA-256 一致。
3. 已使用 UTF-8 简体中文提交信息提交功能改动。
4. 已推送 `codex/task-prompt-panel` 到个人 fork `mofei-114`。
5. 已创建目标为上游 `main` 的中文 PR：<https://github.com/menglimi/astrbot_plugin_private_companion/pull/235>，共 23 个文件、+5949/-113。

PR 状态核对结果：目标分支 `main`、来源分支 `mofei-114:codex/task-prompt-panel`、`mergeable=MERGEABLE`、`mergeStateStatus=CLEAN`，三个 CI 任务 `architecture-and-artifact`、`contracts-and-migrations`、`core-only` 全部通过；标题与正文经 UTF-8 复核无乱码。

变基后重新执行了本文档「已完成验证」一节的测试集合，并追加通过 `tests/test_hdsi_experiment.py` 与 `tests/test_group_social_contracts.py`，结果为 `214 passed, 59 subtests passed, 1 warning`；静态检查亦全部重新通过。

此后维护本功能时，应沿用上述清单：任何新增任务都必须同时更新 `task_prompt_registry.py` 与对应回归测试，再重新执行「已完成验证」一节列出的测试文件。

## 变更边界提醒

任何后续维护都不得把主对话系统提示词纳入此面板，也不得为了“全量覆盖”而抓取或修改 AstrBot 主对话配置。这里的全量仅指本插件内部任务模型；新增任务时应先确认其属于插件内部任务，再加入注册表和测试。

识屏边界需要单独注意：`_goodnight_screen_check_prompt_document()` 和 `_screen_peek_prompt_document()` 虽由 `proactive_message.py` 生成，但只作为 `request_prompt` 传给外部 `astrbot_plugin_screen_companion._invoke_screen_skill()`；实际视觉模型、Provider 和任务生命周期由外部插件负责。因此 `screen_peek`、`goodnight_screen_check` 以及 `private_companion_screen_peek`、`private_companion_goodnight_screen_check`、`private_companion_troubleshooting_screen_peek` 均不属于本面板可管理任务。核心插件自己的 `screen_narration`（工具结果转述（识屏））仍属于本面板，并必须完整展示和支持附加指令。
